{
  "name": "nexuscrm",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client start"
  },
  "devDependencies": {
    "semver": "^7.6.3"
  }
}
